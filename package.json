{
  "name": "job-extractor",
  "version": "1.0.0",
  "description": "Extract job data from Indeed URLs",
  "main": "extract_jobs_bulk.js",
  "scripts": {
    "html-to-json": "node scripts/html_to_json.js",
    "json-to-excel": "node scripts/json_to_excel.js",
    "start": "npm run html-to-json && npm run json-to-excel"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "xlsx": "^0.18.5"
  }
}
